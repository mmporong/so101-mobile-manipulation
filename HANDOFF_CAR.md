# SO-101 Mobile Manipulation 차량 장착 인계 (2026-08-27)

노트북에서 수집·학습, 최종 배포는 라즈베리파이. 팔은 JD-AMR 상판에 **클램프**로
임시 고정(볼트 구멍 가공은 나중). 이 문서는 다른 세션이 이어받기 위한 현재 상태다.

정본 저장소: `~/so101-mobile-manipulation`

이전 경로 `~/so101_tools`는 호환 링크로 유지한다.

## 1. 하드웨어 구성

- 팔 SO-101, 상판 **뒤쪽 가장자리**에 C클램프 2개로 고정 (사진: ~/Downloads/1.jpg~3.jpg)
- 상판 높이 = 테이블에서 **160mm**. 팔 어깨 기준점(PAN0)은 상판에서 78mm 위
  → **바닥(테이블) = 팔 기준 z −0.238 m**
- 손목캠만 사용. **뎁스캠(Astra)은 철거** — 정책·파지 모두 손목캠 단독
- 차가 앞으로 쏠려 넘어진 적 있음 → **뒤쪽에 counterweight 적재 중**
- 전원은 벽 어댑터(정차 시험용). 배터리 배선은 파지 완성 후로 미룸

## 2. 팬(shoulder_pan) 제약 — 이 프로젝트의 핵심 제약

- 좌우 회전이 **차체·주변과 충돌**할 위험이 있어 제한 중 (클램프 비틀림이 아니라 **충돌**)
- 패널에 `pan_lock` op 구현: 잠긴 각도 **±tol** 밖의 명령을 goto·IK·pose 세 경로에서 차단
- ✅ **팬 재측정 완료 (2026-08-27)**: 충돌 직전까지 실측 → **−24.3° ~ −6.9°**
  (표기값 기준 폭 17.4°, 중심 **−15.6°**). 처음 손으로 훑은 −32.5~+40.2° 는 충돌을 고려하지
  않은 값이라 폐기했다.
- 현재 설정: **중심 −15.6° ± 7.0°** (실측 반폭 8.7° 에서 1.7° 안전 여유).
  `run_batch.sh` 가 배치 시작 때 자동으로 이 값으로 잠근다.
- `pan_lock` op 은 `center` 를 받는다 — 팔을 옮기지 않고도 잠금 중심을 지정할 수 있다:
  `{"op":"pan_lock","on":true,"tol":7.0,"center":-15.6}`
- tol 과 좌우 작업폭: ±5°→±17mm · **±7°→±23mm** · ±8.7°→±29mm (반지름 190mm 기준)

## 3. 교시값 (차량 기하, 2026-08-26 재교시)

| 항목 | 값 | 비고 |
|---|---|---|
| floor_z_m | −0.238 | 상판 160mm + 어깨 78mm |
| 관찰 높이 / 목표 픽셀 | −0.169 (바닥+69mm) / (168.7, 171.1) | |
| 파지 높이 / 기준 픽셀 | −0.233 (바닥+5mm) / (96.4, 190.1) | |
| 반지름 이득 | dcx/dr −1518, dcy/dr +108 px/m | 벤치 −2217 대비 68% |
| 파지 반지름 오프셋 | −6mm | "큐브가 아랫턱을 파고든다" 대응 |
| 팬 잠금 기준 | 그때그때 현재 각도 | TCP 방위각 ≈ +21.5° (부호 규약 다름 주의) |

⚠ **교시 재확인 필요**: 마지막 시험에서 큐브가 목표에서 105px 벗어나 보였다.
팔을 손으로 옮긴 뒤라 기준이 어긋났을 수 있다. 재교시 절차는 5번 참조.

## 4. 작업 가능 영역 (계산, 팬 ±5° 기준)

- (팬 ±5° 기준) 반지름 **100~240mm**, 좌우 **±9~±21mm**
- **팬 ±7° 로 갱신 후**: 좌우가 약 1.4배 → 반지름 190mm 에서 ±23mm
- 250mm 이상은 관찰은 되나 파지 높이에 못 닿음
- 팬 tol 을 넓히면 좌우가 비례해 넓어짐 (±10° → 좌우 ±42mm)

## 5. 재교시 절차 (기준이 어긋났을 때)

1. 큐브를 작업 위치에 놓고, 팔을 그 위로 (손 또는 IK)
2. `pan_lock` 을 현재 각도로 갱신
3. 팔을 **파지 높이**(바닥+5mm)로 하강, 죠 개방(45)
4. 큐브를 **죠 사이에 손으로 넣고** → `wrist_calib.py --ref` (촬영만)
5. 큐브를 그 자리 테이블에 놓고 → 팔을 **관찰 높이**(바닥+69mm)로 상승 → 관찰 기준 촬영
   (스크립트: 세션 로그 참조, servo_gain.json 의 wrist_obs_* 갱신)
6. (선택) `calib_radial.py` — 반지름 이득 재측정, 팔이 앞뒤로 ±25mm 움직임

## 6. 도구

| 파일 | 용도 |
|---|---|
| `run_batch.sh N` | 배치 1회: 예열→세우기→N사이클→**준비자세 대기**(파킹 안 함) |
| `collect_cycles.py` | 무인 수집 (지터 ±15mm, 품질 게이트, CSV 기록) |
| `pick_wrist.py` | IBVS 파지 (팬 잠금·반지름 이득·롤 선행 하강) |
| `aim.py` | 큐브 좌우 편차 실시간 표시 (관찰 높이에서만 정확) |
| `record_range.py` | 손으로 움직이며 안전 범위 기록 → car_limits.json |
| `calib_radial.py` | 반지름 이득 측정 (팬 안 돌림) |
| `analyze_log.py` | pick_log.csv 분석 (성공률·실패사유·추세) |
| `car_align.py` | 차 회전 정렬각 계산 |

## 7. 안전 장치 (건드리지 말 것)

- **토크 컷은 과전류에서만** — 판정 실패·통신 순단은 `_hold_or_kill`(자세 유지)
- **팬 잠금** 3경로 차단
- **품질 게이트**: 밀림 경고·정렬 오차·파지 판정 실패 시 에피소드 폐기
- 팔 Torque_Limit **1000(해제)**, 그리퍼 45%. 과전류 320 유지
- Goal_Velocity 8 을 쓰는 코드는 **전부 제거됨** (잔재가 스톨 오판을 만들었음)

## 8. 데이터·학습

- 데이터셋 `~/so101_datasets/so101_car` (손목캠 전용) — 현재 **1 에피소드**
- 벤치 데이터(so101_pick_pm 등)는 기하가 달라 **차량 학습에 섞지 말 것**
- 학습: `lerobot-train --policy.type=act --policy.chunk_size=30`, 손목 뷰는 `make_wrist_view.py`
- 실기: `act_run.py --replan 15` (청크 모드). 앙상블 모드는 맴돌기 유발
- 파이 추론 예상 200~300ms/회 → `--replan 15`(1.5초)면 충분

## 8-1. 현재 팔 상태 (2026-08-27 세션 종료 시점)

- 토크 OFF, 접힘 자세 부근. 팬 잠금 −21.3° ± 5.0° 로 걸려 있음(패널 재기동 시 초기화됨)
- 마지막 시험: 1사이클 실패 — 큐브가 목표에서 105px 이탈. 교시 재확인 대상
- `pick_log.csv` 에 사이클 4건 기록(모두 실패). `analyze_log.py` 로 확인 가능
- 105px 이탈은 화면 관찰값이며 현재 CSV에는 직접 기록되지 않았다.

## 9. 다음 단계

1. ~~shoulder_pan 안전 범위 재측정~~ ✅ 완료 (2026-08-27, 중심 −15.6° ± 7.0°)
2. **교시 재확인** — 마지막 시험에서 큐브가 목표 픽셀에서 105px 벗어나 보였다.
   5번 절차로 파지·관찰 기준을 다시 잡을 것. (팔을 손으로 옮긴 뒤라 기준이 어긋났다)
3. 1사이클 검증 → 배치로 수집 확대 (목표 40~50 에피소드)
4. 학습 → 실기 성공률 측정
5. SLAM 연동: 차 회전 정렬은 **큐브가 회전중심에서 멀 때만** 유효 (car_align.py 참조)

## 10. 이 세션에서 바뀐 것 (2026-08-27)

- 팬 잠금에 **허용 범위(tol)** 도입 — 완전 고정 → `중심 ± tol` 클램프.
  goto·IK·pose 세 경로 + `act_run.py`(정책)까지 적용
- 팬 각도 클램프 기준을 **TCP 방위각**으로 수정 — 서보 팬 각(−22.5°)과 TCP
  방위각(+21.5°)은 부호 규약이 달라, 서보 각으로 접으면 목표가 반대편으로 튄다
  (실측: 한 걸음에 113mm 이탈)
- 배치 실패 시 **휴지로 접지 않고 준비(ㄷ자) 자세 유지** — 재시도가 빨라진다
- 파지 후 **더 높이 들어올림**(관찰 높이 +최대 60mm) — 성공/실패를 눈으로 구분
- 사이클마다 **CSV 기록** `~/so101_datasets/pick_log.csv` (`analyze_log.py` 로 분석)
- 새 도구: `record_range.py`(--seconds/--joint/--merge), `analyze_log.py`,
  `car_align.py`, `aim.py`(높이 경고 포함)
