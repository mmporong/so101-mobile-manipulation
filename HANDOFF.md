# SO-101 + Astra 인수인계 (2026-08-19 갱신)

## 0. 현재 상태 — 한눈에

| 항목 | 상태 |
|---|---|
| 서보 6개 | **전부 정상** (ID 5·6 교체 완료), 온도 24~32°C |
| 캘리브레이션 | **완료** (2026-08-19 재실시, 파일·EEPROM 검증) |
| 보호 파라미터 | 전 서보 적용 (그리퍼는 전용값) |
| 안전장치 | 구현 완료. **0번(범위 게이트)·토크 켜기 검사는 실물 검증 완료**(8-19 저녁), 스톨·전류·과온은 미검증 |
| 뎁스캠 | **정상** — 캡처를 별도 프로세스로 분리해 해결. `/points` 로 3D 점 제공 |
| 팔 | **토크 OFF·안착 자세** (2026-08-19 저녁 기준. 서버는 연결 유지) |
| 정합(hand-eye) | **미완료** — 사고로 중단, 코드는 준비됨 |
| 손목캠 상수 | **무효** — 캠 위치 이동으로 재측정 필요 |

**서버**: 실행 중. 다만 마지막 커밋(`66a3b7b` 전류 피크 기록·슬라이더 범위 검사)은
기동 이후라 **재시작해야 반영된다.** 핵심 안전장치는 이미 반영돼 있다.

---

## 1. 하드웨어 사고 — 원인 확정

**`wrist_roll`(ID 5) 서보가 과열·발연해 교체했다.** (초기에 `wrist_flex`로 적었던 것은
오판이다 — ID 5만 빼자 나머지 다섯이 살아나 원인이 특정됐다.) 원인은 세 겹이고
전부 코드 쪽 설계 공백이다.

### ① 눌린 죠에 회전을 요구했다 (순서 문제)

`wrist_roll`이 97.98°(누운 자세)라 죠가 좌우로 벌어진 채 팔이 내려가 **한쪽 턱이
책상에 닿았고**, 그 상태에서 IK가 roll을 180°로 돌리라고 명령했다. 눌린 죠는 돌 수
없다 — 82°를 남기고 스톨.

`floor_z_m`(-0.1037)은 특정 roll에서 잰 값인데 "roll이 어떻든 여유가 있다"고
가정한 것이 화근이었다. roll이 90° 누우면 죠 기하가 달라져 닿는 높이가 바뀌고,
죠에 물체까지 물려 있었다.

### ② 보간 루프가 서보 자체 보호를 무력화했다

STS3215 데이터시트 7-11 Electronic Protection:

> **Overload**: 스톨 토크의 80%를 2초 초과하면 보호 진입. **위치 명령을 다시 보내면
> 보호 플래그가 해제된다.**
> **Over Current**: 동작 전류가 2A를 2초 초과하면 출력을 끄고 보호 진입.
> **위치 명령 재전송으로 플래그가 해제된다.**

`_do_move_q`는 **20ms마다 `Goal_Position`을 쓴다.** 서보가 2초를 견디고 스스로
출력을 껐는데 초당 50번 "다시 가라"고 명령해 보호를 계속 풀었다. 막힌 상황에서
`_do_stop()`처럼 현재 위치를 다시 쓰는 것조차 같은 해제 효과를 낸다.

### ③ 도달 실패를 감지하고도 힘을 빼지 않았다

정합 스크립트가 13개 지점 이동을 걸었고 전부 도달에 실패했는데, "건너뜀"만 출력하고
정지 명령을 보내지 않았다. 서보 목표가 그대로 남아 계속 밀었다. 그 상태가 장시간
유지됐고, 발견 시점에는 통신이 끊겨 소프트웨어로 토크도 못 껐다(전원 차단으로 정지).

**교훈**: 실패를 기록하는 것과 안전한 상태로 되돌리는 것은 다른 일이다.

---

## 2. 안전장치 (6종) — 실물 검증 미완

방어 순서대로:

| # | 장치 | 시점 | 위치 |
|---|---|---|---|
| 0 | **캘리브 범위 검사** | 명령 전 | `arm_gui._clamp_to_calib` |
| 1 | **회전 우선 분할** | 명령 시 | `arm_gui._do_move_q` |
| 2 | 서보 펌웨어 보호 | 즉시 | 연결 시 EEPROM 기록 |
| 3 | 전류 감시 | 0.2초 | `arm_gui._interp` |
| 4 | 스톨 감지 | 0.5초 | `arm_gui._interp` |
| 5 | 과온 감시 | 6초 | `arm_gui._guard` |

**0번이 가장 중요하다.** IK는 URDF 관절 한계로 해를 내는데 그 값이 서보 실측 범위보다
넓다(재캘리브 후 실측):

```
shoulder_pan   서보 ±98.9    URDF ±110.0   → 양끝 11.1° 초과
elbow_flex     서보 ±97.6    URDF  ±96.8   → 초과 없음 (0.8° 여유)
```

**✎ 2026-08-19 저녁 정정**: 종전 표기(-88.4~109.3 / -107.8~87.4)는 중립 2048 고정으로
잘못 환산한 숫자였다. lerobot DEGREES 정규화는 **범위 중점 기준·360/4095** 라 서보
한계는 항상 ±대칭이다 — 경계 환산은 반드시 `arm_lib.calib_bounds()` 하나만 쓸 것.
(잘못된 식은 shoulder_pan 에서 12.5° 오차단·8.4° 과허용을 만들고 있었다.)

범위 밖을 명령하면 서보는 갈 수 있는 데까지 가서 **나머지를 계속 민다** — 사고와
같은 구조다. 이제 이동을 시작조차 하지 않는다(양 끝 2° 여유).

**1번**: `wrist_roll` 변화가 20°를 넘으면 나머지를 그대로 둔 채 **roll만 먼저** 돌리고
본 이동을 한다. 회전이 막히면 본 이동을 아예 하지 않는다.

### 2026-08-19 저녁 추가분 (가짜 서보 테스트 통과, 실물 미검증)

- **토크 켜기 검사** (`_do_torque`): 켜기 전에 목표를 현재 위치로 덮고, 현재 자세가
  캘리브 범위를 크게 벗어났으면(바깥 3° 허용) 거부한다. "토크를 켜는 것 자체가 위험
  동작" 교훈의 코드화. **검사·기록은 raw 카운트로 한다** — 정규화 읽기는 그리퍼에서
  범위 밖을 0/100 으로 클램프해 버려 범위 밖을 볼 수 없고, 그 값을 되쓰면 켜는 순간
  스스로 움직인다. 인가 루프 실패 시 전체를 도로 내린다. `_do_grip_test` 도 동일 +
  그리퍼 통전 시 `state['torque']` 를 세워 `_guard` 감시가 붙게 했다(종전엔 파지
  유지 중 무감시 통전).
- **조그·그리퍼 범위 검사**: `_do_jog` 가 캘리브 범위를 검사한다(종전엔 goto·move_q만).
  그리퍼는 jog/goto/grip_test/stop_test 전부 0~100 으로 묶었다. `stop_test` 도 같은
  게이트 + 대기 상한 3초 + 0.1초 슬라이스로 abort·과전류 확인(종전엔 검사·상한·중단
  전부 없어 사고 조건을 재현할 수 있었다).
- **fail-closed**: `_clamp_to_calib` 는 캘리브 파일을 못 읽으면 거부한다(종전엔 통과).
  이동 중 읽기 실패는 `_do_stop`(위치 재전송 = 보호 해제) 대신 `_kill_torque` 로 끊는다.
- **스톨 감지를 관절별로** (`_interp`): 종전엔 전체 max 기준이라 **한 관절만 막히고
  나머지가 움직이면 감지 못 했다 — 사고가 정확히 그 모양**(wrist_roll만 막힘)이었다.
  이제 관절별로 "0.5초간 안 움직였는데 보간 목표가 3° 이상 앞서 있으면" 즉시
  `_kill_torque`. 비교 기준을 최종 목표가 아닌 현재 보간 목표로 바꿔 초반 오탐도 없앴다.
  **기는 스톨**(마찰로 1°/s 씩 밀리는 부분 스톨)도 뒤처짐 15° 에서 끊되, 속도 상한이
  허용하는 만큼 움직이고 있는 관절(정상 저속 이동)은 면제한다.
  가짜 버스 재현: 사고 시나리오 1초 내 감지, 정상 이동 오탐 없음.
- **무효 상수 가드** (`arm_lib.load_gain`): `servo_gain.json` 의 `stale_*` 목록에 있는
  키를 요구하면 실행을 멈춘다. `pick_red.py`(floor·y_to_px·기준선)·`align_y.py`(y_to_px)
  가 이걸로 로드해 **재측정 전에는 돌지 않는다**. `floor_z_m`·`cam_up_roll_deg` 를
  `stale_after_recalib` 로 표시했다.
- **handeye 프리플라이트**: 이동 시작 전에 13점 전부를 IK+캘리브 범위로 정적 검증하고,
  floor_z_m 이 stale 이면 거부한다(`--force-floor` 로만 강행).

리뷰(2패스, APPROVE) 잔여 Minor 5건 — 안전 영향 없음, 다음 사이클:
- `state['torque']` 가 "그리퍼만 통전"과 "팔 전체 통전"을 구분 못 한다 → grip_test 직후
  IK 이동이 게이트를 통과하고 팔이 안 움직여 "⛔ 스톨"로 끝난다(움직임 없음·토크 차단이라
  안전한 방향). **grip_test 직후 뜨는 스톨 메시지는 간섭이 아니라 팔 토크 미인가일 수
  있다.** 플래그 분리(`gripper_torque`)가 정석.
- `_do_neutral` 이 `_calib_cache` 를 안 비운다 (호밍이 raw 프레임을 옮긴다) — 한 줄 추가.
- `_do_stop_test` 의 abort 분기가 try 밖 / 전류 읽기 실패 시 `c=0` 으로 감시가 조용히
  꺼진다(3초 상한이라 노출 제한적).
- `calib_bounds` 가 관절 누락 캘리브에서 KeyError 트레이스백(이동 전이라 fail-closed).
- 실물 첫 주행 때: 스톨 로그에 `moved/win_cap` 비율을 남기면 기는-스톨 면제의 오탐
  여부를 로그만으로 판별할 수 있다.

**스톨·과전류 정지는 `_kill_torque()`를 쓴다** — 위치 명령을 보내지 않고 토크를
끊는다. 위치 명령은 서보 보호를 해제시키므로 `_do_stop()`을 쓰면 안 된다.

**✎ 정지(stop)는 이제 ARM 5관절만 붙든다 — 그리퍼는 안 멈춘다.** 그리퍼 목표를
현재 위치로 덮으면 물체를 물던 **파지 예압이 사라지기** 때문에 의도적으로 뺐다
(2026-08-19 감사 M2). "정지를 눌렀는데 죠가 계속 움직인다"는 버그가 아니다 —
그리퍼를 멈추려면 토크 OFF. 또한 stop 은 속도 상한을 8로 내리며, 복원은
`_do_move_q` 가 이동 시작 전에 자동으로 한다(C1).

### 임계값 (`arm_gui.py` 상단)

```
TEMP_WARN, TEMP_STOP = 55, 62      # °C (STS3215 기본 한계 70)
CURRENT_STOP = 250                 # ~1.6A — 12V 정격 900mA 의 1.8배
CURRENT_HOLD = 2                   # 연속 관측 횟수
STALL_GAP_DEG = 3.0
STALL_MOVE_DEG = 0.4
ROLL_FIRST_DEG = 20
LIMIT_MARGIN_DEG = 2.0
```

**★ `CURRENT_STOP`은 아직 추정치다.** 데이터시트(12V 30kg: 무부하 180mA · 정격
900mA · 스톨 2.7A) 기반이고 **실측하지 않았다.** 너무 낮으면 정상 동작에서 오정지하고,
너무 높으면 보호가 안 된다. 이동 로그의 `전류피크` 값을 몇 번 모아 조정할 것.

**정지 상태에서는 전류가 0이다**(감속비 1/345라 중력을 기어가 버틴다). 기준선은
반드시 움직이는 중에 재야 한다. `_guard`(6초 주기)로는 짧은 이동의 피크를 놓치므로
`_interp`가 기록하는 `state['last_peak']`를 봐야 한다.

### 서보 보호 파라미터 (연결 시 자동 기록)

```
표준(ID 1~5): 과온 65도 · Protection_Current 320(~2.1A) · Overload_Torque 80%(공장값)
              · Protection_Time 2초(공장값) · Protective_Torque 20%
✎ 2026-08-20 밤 정정: 종전 60%·0.5초는 정상 저속 이동에 오발해 "버스 급사"
  4회를 만든 진범으로 확정 — 과부하 계열은 공장값 복원 (커밋 e441726)
그리퍼(ID 6): 과온 65도 · Protection_Time 0.5초 · Overload_Torque 25%
              · Protection_Current 250
```

그리퍼를 낮게 잡는 이유: **물체를 잡고 계속 힘을 주는 것이 정상 동작**이라 임계를
다른 관절처럼 올리면 물체를 부수거나 서보가 무리한다. 이 팔의 원래 그리퍼가 25%로
출하됐고 그 의도를 존중했다.

---

## 3. 캘리브레이션 (2026-08-19 재실시)

`wrist_roll`(ID 5)·`gripper`(ID 6) 교체 후 전 관절 재실시. **중립 자세는 아랫턱
우측 · 캠 위** — 아랫턱이 아래를 향하는 자세는 가동범위에 걸려 만들 수 없다.
손목캠이 그리퍼 **옆면**에 달려 있어 아랫턱이 아래면 캠이 우측을 본다.

```
관절            homing   range        (raw, 중립 2048)
shoulder_pan      1847   1042~3292
shoulder_lift    -1224    765~3328
elbow_flex        1675    822~3042
wrist_flex          21    896~3192
wrist_roll        -945     14~4018    (거의 360도 전체)
gripper            836   2026~3945
```

파일: `~/.cache/huggingface/lerobot/calibration/robots/so_follower/follower.json`
(캐시 경로라 유실 위험 → 검증 사본이 `config/calibration_follower.json`에 커밋돼 있다)
이전 값 백업: `~/so101-mobile-manipulation/calib_backup_*.json`

**★ `mapping.json`의 `offsets.wrist_roll`을 -180 → 0으로 되돌렸다.** 어제 -180을
넣은 이유는 "서보 중립이 캠-아래 자세로 잡혀 있어서"였는데 재캘리브로 원인이
사라졌다. 오프셋을 남기면 IK 목표가 **항상 ±180 경계**에 놓여, 0.26도 차이를 359.74도
역회전으로 수행하는 문제가 상시화된다(사고의 한 축).
검증: IK(x=0.20, y=0, z=0.02) → `wrist_roll` 목표 `0.00`.

**✎ 캘리브가 바뀌었으므로 `servo_gain.json`의 `floor_z_m`(-0.1037)도 무효다.**
FK 좌표계 기준이 달라졌다. 책상 높이를 다시 재기 전에는 낮은 z 목표를 주지 말 것.

---

## 4. 서보 교체 절차 (도구 있음)

새 서보는 **팔에 조립하기 전에 단독으로 물려 ID를 먼저 바꾼다.** 공장 기본 ID가 1이라
그대로 꽂으면 기존 `shoulder_pan`과 충돌해 **버스 전체가 먹통**이 된다.

```
python3 ~/so101-mobile-manipulation/servo_check.py                 # 읽기 전용 진단
python3 ~/so101-mobile-manipulation/servo_id.py --to 5             # ID 부여 + 보호 파라미터
python3 ~/so101-mobile-manipulation/servo_id.py --protect-only 6   # ID 변경 없이 보호만
```

`servo_id.py`는 버스에 서보가 **정확히 하나**일 때만 진행한다(둘 이상이면 중단).
`servo_check.py`는 위치 제한이 공장값(`0~4095`)이면 새 서보로 판정한다.

**단락 진단**: 서보 하나가 내부 단락되면 데이지체인 전원 라인을 끌어내려 **전 서보가
무응답**이 된다. 어댑터 LED가 깜빡이면 그 신호다. 서보를 하나씩 물려 가며 좁힌다.

**✎ 판매자가 ID와 위치 제한을 미리 설정해 보내기도 한다.** 이번 교체품은 ID 3·5·6으로
왔고 위치 제한도 들어 있었다. `servo_check.py`로 먼저 확인할 것.

---

## 5. 뎁스캠 — 해결됨

**원인은 스트림 코드가 아니라 SDK 교착이었다.** Legacy Astra SDK는 같은 USB2 버스의
UVC 캠이 열리는 순간 `astra_update()` 안에서 **영구 교착**한다. 교착 후에도 HTTP
핸들러는 살아 있어 **캐시된 마지막 JPEG를 계속 보내므로** "브라우저에서만 멈춤"으로
보인다. `/depth`만 단독으로 읽으면 정상인데, 그때는 `/cam`이 안 열려 교착이 없기
때문이다.

`depth_daemon.py`가 캡처를 독립 프로세스로 돌린다:

```
GET /all    → {seq, beat_age, stats, blob, depth_jpeg, rgb_jpeg}
GET /health → {seq, beat_age}
```

`beat_age`가 큰 것은 "SDK 안에서 멈춤"만을 뜻한다 — 교착을 정확히 판정하는 계기다.
`panel_server`의 감독 스레드가 하트비트가 끊기면 데몬만 갈아끼운다.

**뎁스캠 전용 빨강 HSV**(손목캠 값을 그대로 쓰면 검출 0):
`[((0,140,55),(10,255,255)), ((166,140,55),(179,255,255))]`
같은 장면 실측 — 물체 `H 168~175 · S 중앙 211 · V 중앙 87`, 사람 팔 `H 5~6 · S 117`.
`V` 하한 100이 물체를 통째로 잘랐고, `S>=140`이면 살색이 전부 빠진다.

**깊이 창 확장**: 구조광이라 물체 주변에 그림자가 생겨 작은 큐브가 통째로 들어간다.
고정 11x11에서는 유효 화소 0 → `cam_xyz=None`. `r = 5 → 9 → 14 → 20`으로 넓힌다.

---

## 6. 정합(hand-eye) — 1차 순회 완료(무사고), 정합값은 무효

**2026-08-19 밤 1차 순회 결과** — **13점 무사고 완주** (도달 오차 1~6mm, 스톨·킬·
이탈 0회). 안전장치 전체가 실전에서 검증됐다. 그러나 **정합값은 못 얻었다**:

- **원인 1 — 물었던 빨간 물체가 얇아 구조광 깊이에 안 잡혔다.** 컬러 250px 에
  유효 깊이 화소 22~44개(4%), 깊이 창이 r=14 로 넓어져 배경(책상) 중앙값을 반환.
  깊이 잔차 std 40mm → Kabsch RMS 33mm. read_blob 에 품질 게이트(win_r≤9·
  valid_px≥8)를 넣었다 — 이제 이런 관측은 자동 거부된다.
- **원인 2 — 시야에 다른 빨간 물체가 있어** 블롭이 교대로 잡힘(z 650↔810 실측).
- 구제 시도 전부 실패: 외란 제거·스케일 적합·방위각 PnP(모호성: 책상 법선 83°)·
  방위+평면 결합·내부파라미터·죠 오프셋 추가 — 서로 다른 방향으로 발산.
  **모델을 더 씌우지 말고 측정을 고칠 것.**
- 무효 결과는 `~/so101-mobile-manipulation/handeye_invalid_pnp_20260819.json` 격리,
  원본 데이터는 `handeye_kabsch_rms33.json.bak` (방위각 13점은 2.5px 일관 — 재사용
  가능). **handeye.json 은 없음** = floor_from_depth 가 로봇 좌표를 내지 않는 상태.

**✎ 물체 정체 확인(사용자)**: 세로로 긴 **체스말형** 빨간 물체를 위쪽으로 물고
있었다. 그러면 실패가 하나로 설명된다 — ① 좁고 둥근 몸통이라 구조광 유효 화소가
4%뿐(깊이는 배경을 반환) ② 밑동이 TCP 아래로 길게 늘어져 **아래층(z=-0.03,
여유 48mm)에서 책상에 끌리며 죠 안에서 밀렸을** 개연성: 하층 방위 잔차가 상층의
1.5배(5.7 vs 3.7mrad, 3·5번 지점 9~12mrad), 순회 중 그리퍼가 11.3→1.1 로 조여짐.
MIN_CLEAR 의 "돌출 ~20mm" 가정이 이 물체에는 틀렸다.

**★ 2026-08-20 새벽 — 진범 추가 확정: Astra 이미지가 좌우 거울상이었다.**
SDK 기본 미러를 astra.py 가 해제하지 않아 깊이·컬러 전부 거울상 좌표 → 거울상은
어떤 회전으로도 실세계와 정합 불가(카메라 위치가 "지하 -0.5m"로 발산하던 이유).
astra.py 에서 해제했고(이후 픽셀·cam_xyz·/points 전부 새 규약), 끌림 없던 상층
7점 방위각(u 반전 변환) + 해제된 라이브 책상 평면으로 **잠정 정합을 저장**
(`~/so101-mobile-manipulation/handeye.json`, provisional=True):
- 방위 잔차 2.2px · 카메라 위치 (-0.10, -0.33, +0.35)m — **실물 배치와 맞는지 확인 요망**
- 검증: floor_from_depth 작업영역 z = -0.060 (실제 -0.078, **오차 18mm**) —
  대략 위치용으론 쓰되 **파지용으론 재순회 필수**. 미러 시절 저장 데이터를 쓸 땐
  u 부호 반전 필요(handeye_kabsch_rms33.json.bak 이 그 형식).

**재시도 절차 (2026-08-20 준비 완료 — 체스말 그대로 사용 가능)**:
handeye 가 **이중 솔버**가 됐다: 픽셀 방위각은 깊이와 무관하게 항상 수집하고,
[Kabsch](깊이 통과 관측 ≥6)와 [방위각+책상평면](항상)을 각각 게이트 후 교차
대조한다(불일치 시 저장 거부). **고무 체스말처럼 깊이가 전멸해도 방위각+평면
으로 풀린다** (E2E ⑥: 깊이 0프레임에서 회전 0.30°·이동 2.0mm 복원).
POSES 는 돌출 4cm 실측 기준 아래층 -0.01(밑동-책상 28mm)·x 0.15~0.26 확장.

1. 팔 USB(시리얼) 전원 인가 → 서버 기동 → `handeye.py --dry` (데몬 예열 겸
   관측 확인 — 방위·깊이 프레임 수가 각각 나온다)
2. **시야에서 다른 빨간 물체 전부 제거** (블롭이 오간다 — 1차 순회 오염 원인)
3. 팔 전체 토크 ON → 체스말은 물린 그대로 → pan ±6° 물림 확인 → 순회
4. **실행 중 볼 것**: ① 출력되는 `카메라 위치(로봇 좌표)` 를 줄자로 대조(마지막
   독립 방어선) ② `[방위+평면]` 줄의 `작업면 안 %` 가 100 근처인지 ③ `[Kabsch]`
   가 나오면 특이값·RMS·`[교차]` 차이를 기록 ④ `물체 이탈 의심` 은 오탐률이
   낮으니(검증 0/12) 실제 이탈로 간주
5. 완료 후 `floor_from_depth.py` 로 바닥 교차검증(-0.078 일치 확인)

**목표**: `p_rob = R · p_cam + t`. 죠에 물린 빨간 물체로 대응쌍을 모아 Kabsch(SVD)로
푼다. 물체가 죠에서 차지하는 상대 위치는 상수라 `t`에 흡수되므로, 결과는 곧
"카메라가 본 물체 → 그것을 물 TCP 목표"가 된다.

**세 번 실패했고 원인은 전부 도달 판정이었다**(`ce1e4d0`에 수정 반영, **미검증**):

1. IK 목표를 로봇 좌표로 썼다 → 실제 관절값을 읽어 FK로 구하도록 수정
2. "관절 변화가 멈추면 도착"으로 판정 → 명령 직후엔 출발 전이라 변화가 0이다.
   **목표 관절각과의 거리**로 판정하도록 수정
3. 각도 래핑 누락 → `wrist_roll` 목표 -180과 실제 +180이 278도 차이로 읽혔다.
   `(a-b+180)%360-180`으로 정규화

### 재개 절차 (2026-08-19 종합 감사 반영 체크리스트)

1. **`handeye.py --dry` 를 먼저** — 깊이 데몬은 첫 /blob 요청에 지연 기동하므로,
   안 하면 1번 지점 관측이 실패한다
2. **팔 전체 토크 ON 후** 그리퍼 열고 빨간 물체를 물린다 (grip_test 만으로 물면
   `state['torque']` 는 서지만 팔 다섯 관절이 무토크라 첫 이동에서 스톨 킬 —
   그때 `_kill_torque` 가 전 모터를 풀어 물체까지 떨어진다)
3. **물렸는지 확인** — `shoulder_pan` ±6도에 `/blob`의 `cam_xyz`가 따라오면 물린 것.
   순회 중에도 스크립트가 "로봇은 움직였는데 카메라가 제자리"를 감시해 이탈 시 중단
4. **첫 이동을 작게** — 안착 자세에서 1번 지점까지 한 번에 가지 말고, 패널 IK 로
   상층 근방(예: 0.19, -0.03, +0.02)에 먼저 세운 뒤 시작. 속도 25% 권장(실측 검증됨)
5. `python3 ~/so101-mobile-manipulation/handeye.py` — 프리플라이트(IK·범위·바닥 여유)가 자동으로
   먼저 돈다. **팔이 움직이는 동안 자리를 뜨지 말 것**
6. 이상하면 **정지(SPACE)가 아니라 토크 OFF** — stop 은 위치 재전송이라 서보
   펌웨어 보호 플래그를 해제한다
7. MJPEG 탭(/cam /depth /rgb)을 여러 개 열지 말 것 — 스코프 MemoryMax=1G
8. 결과 `~/so101-mobile-manipulation/handeye.json`. **RMS 잔차와 점 분포 특이값을 볼 것.**
   최소 특이값 0.02 미만이면 평면 퇴화(계획 13점 기준 계산값 0.069 — 여유 3.4배)
9. 정합 직후 `floor_from_depth.py` 로 바닥 비접촉 재측정 → 접촉값(-0.078)과 대조

**✎ 지점 좌표(`POSES`) 도달성은 2026-08-19 저녁 정적으로 재검증했다** — 신규 캘리브
범위 기준 13점 전부 IK 해 있음·범위 안(wrist_roll 목표 0.0). `handeye.py` 가 시작 전에
같은 검증(프리플라이트)을 스스로 돌린다. **남은 위험은 아래층 `z=-0.05` 하나다** —
"책상에서 5cm 여유"의 근거인 `floor_z_m`이 무효라, floor 재측정 전에는 스크립트가
스스로 거부한다(`--force-floor` 로만 강행).

---

## 7. 무효가 된 실측 상수

손목캠 위치 이동과 재캘리브로 아래가 전부 무효다
(`servo_gain.json`의 `stale_after_cam_move` · `stale_after_recalib`에 기록 —
`arm_lib.load_gain(키)` 로 로드하면 stale 키는 실행이 자동으로 막힌다):

- `y_to_px = -4578 px/m` (좌우 서보 이득)
- `jaw_offset_z_m = -0.05`
- 화면 기준선 `306 px`
- `taught_grasp` (캠-하단 시절 교시 자세, `stale: true`)
- ~~`floor_z_m = -0.1037`~~ → **-0.078 로 재측정 완료** (2026-08-19 저녁, 10절 참조)
- `cam_up_roll_deg = -180` (재캘리브로 중립이 바뀜 — 지금은 캠-위가 roll 0 근처)

---

## 8. 반복해서 발목을 잡은 것들

- **`systemd-run --user --scope`로 띄운 프로세스는 `kill`로 안 죽는다.** `pgrep -f`가
  잡는 건 래퍼 bash다. 죽인 줄 알았던 서버가 한 시간 넘게 살아 카메라를 물고 있었다.
  `systemctl --user list-units --type=scope | grep <명령>` → `stop`, 포트가 비었는지로
  교차 확인할 것.

- **USB 리셋 ioctl(`USBDEVFS_RESET`)을 원인 좁히기 전에 걸지 말 것.** 장치가 열거조차
  못 하는 상태(`error -71`)로 굳어, 드라이버 unbind/bind · authorized 토글 · 포트
  VBUS 차단 40초 · xhci 컨트롤러 전체 리셋 어느 것으로도 복구되지 않았다.

- **정적 장면에서는 깊이 프레임이 바이트 단위로 동일하다**(mm 양자화). 카메라가 11fps로
  멀쩡히 돌아도 화면은 정지와 구분되지 않는다. → 깊이 화면 좌상단에 시각·fps를 그렸다.

- **`/tmp` 백업으로 롤백하지 말 것.** 임시 실험 전 백업이 오래된 버전이어서, 되돌리며
  깊이 창 확장·shutdown join·fps 오버레이가 통째로 유실된 채 커밋됐다.

- **다른 세션이 같은 파일을 편집 중일 수 있다.** `panel_server.py`를 커밋하려다
  다른 세션의 `depth_daemon` 작업을 덮을 뻔했다. `git diff --cached`로 내 변경만
  올라갔는지 확인할 것.

- **토크를 켜는 것 자체가 위험 동작이다.** 부하 측정을 "읽기 작업"으로 취급해 토크를
  켰다가 그리퍼가 범위 밖(위치 제한 이탈)이라 스스로 움직여 기구가 물렸다.
  켜기 전에 **목표를 현재 위치로 먼저 쓰고**, 범위 안인지 확인할 것.

---

## 9. 파일 위치

**실행 경로는 `~/so101-mobile-manipulation/`이다.** `robot-dashboard`의
`projects/so101-arm/tools/`는 사본이며, `panel_server.py`가 정본 경로를 import한다.
수정 후 반드시 복사할 것.

| 파일 | 역할 |
|---|---|
| `panel_server.py` (리포) | HTTP 서버. `/state` `/cmd` `/cam` `/depth` `/rgb` `/blob`. 깊이 데몬 감독 |
| `depth_daemon.py` (리포) | 깊이 캡처 전용 프로세스 (포트 8766). SDK 교착 격리 |
| `~/so101-mobile-manipulation/arm_gui.py` | 시리얼 워커. 토크·조그·IK 이동·정지·**안전장치 6종** |
| `~/so101-mobile-manipulation/arm_lib.py` | 매핑·운동학 로드, 관절각 <-> 서보각 변환 |
| `~/so101-mobile-manipulation/astra.py` | Astra ctypes 바인딩 |
| `~/so101-mobile-manipulation/handeye.py` | 정합 스크립트 (미검증) — 프리플라이트가 IK·범위·**바닥 여유**를 정적 검사 |
| `~/so101-mobile-manipulation/floor_from_depth.py` | **비접촉** 바닥 측정 — 뎁스 `/points` 평면 피팅, 정합 후 로봇 z 환산 |
| `~/so101-mobile-manipulation/unfold_safe.py` | 접힌 팔 펴기 — FK 안전 웨이포인트를 15Hz 연속 궤적으로 실행 |
| `~/so101-mobile-manipulation/collect_peaks.py` | 정상 이동 전류 피크 수집 (CURRENT_STOP 보정용) |
| `~/so101-mobile-manipulation/servo_check.py` | 서보 진단 (읽기 전용) |
| `~/so101-mobile-manipulation/servo_id.py` | 교체 서보 ID 부여 + 보호 설정 |
| `~/so101-mobile-manipulation/mapping.json` | 부호·오프셋·홈 자세 |
| `~/so101-mobile-manipulation/servo_gain.json` | 실측 상수 (상당수 무효 — 7절 참조) |

**서버 기동**:

```
systemd-run --user --scope -q -p MemoryMax=1G bash -c \
  'export PYTHONPATH=; export LD_LIBRARY_PATH=$HOME/AstraSDK/lib:$LD_LIBRARY_PATH; \
   cd ~/robot-dashboard/projects/so101-arm && exec ~/miniforge3/envs/lerobot/bin/python -u panel_server.py'
```

`panel_server`가 `depth_daemon.py`를 자동으로 띄우고 감독한다.

기동 후 `{"op":"connect"}` → 속도 낮추고 → `{"op":"torque","on":true}` → 즉시 `{"op":"stop"}`.
연결 시 `disable_torque()`가 불려 팔이 처지므로 미리 낮은 자세로 두고, 토크를 켤 때
이전 목표로 튀는 것을 `stop`으로 막는다.

---

## 10. 다음에 할 일 (우선순위)

1. ~~안전장치 실물 검증 (0번)~~ — **완료 (2026-08-19 저녁, 서버 재시작 후 실물 확인)**:
   - `goto shoulder_pan=105°` → `⛔ 거부 — 캘리브 상한 96.9° 밖`, 이동량 0.0°.
     **이 105°는 교정 전 식(상한 107.3)에서는 통과했을 값** — C1 수정의 실물 확인.
   - IK `(x=-0.021, y=0.118, z=0)` → 해는 나오지만(shoulder_pan -100.1°)
     `⛔ 이동 거부`, 이동량 0.0°, 토크 유지.
   - 토크 켜기 raw 검사도 실증: elbow_flex 가 상한 바깥 1.3°(바깥 허용 3° 이내)에서
     정상 인가됐고, **펌웨어 Max_Position_Limit 가 목표를 클램프해 1.2° 범위 안쪽으로
     당겨지는 것**을 실측 — 리뷰 Open Question("펌웨어가 실제로 클램프하나")이 사실로
     확인됨. 범위 밖 목표는 계속 밀리는 게 아니라 한계에서 선다. 단 그만큼 "한계에서
     오차 0으로 서므로" 스톨 감지가 아니라 게이트가 유일한 방어라는 점은 그대로다.
   나머지 실물 검증(스톨 킬·과전류·과온)은 간섭을 일부러 만들어야 해서 별도 계획 필요.
2. ~~전류 임계 실측 (무부하)~~ — **완료**: `collect_peaks.py` 7회 이동, 피크 최대
   10 (0.07A). `CURRENT_STOP=250` 유지 — 정격 부하 138 이라 **부하 이동 실측 전에는
   내리지 말 것** (arm_gui 주석에 기준선 기록). 파지 성공 후 부하 피크 수집이 다음.
3. ~~`floor_z_m` 재측정~~ — **완료: -0.078** (stale 해제). 두 독립 관측의 일치 —
   probe 하강 중 부하 **하락**(-0.076~-0.078, 책상이 팔을 받치며 중력 부하 감소)과
   토크 OFF 안착 자세 FK(-0.0785).
   **✎ probe 1차 실행 사고**: 절대 임계(기준선+200)가 접촉을 3.6cm 지나쳐 -0.120
   까지 눌렀다(사용자 중단). 접촉의 첫 신호는 상승이 아니라 **하락**이다 — 판정을
   롤링 중앙값 대비 ±편차·연속 2회로 재설계했고 실측 리플레이로 42mm 일찍 멈춤을
   확인. **눌림이 지속되면 버스가 죽는다**(당일 2회: id1 쓰기 무응답 → 전 서보
   무응답, 복구는 전원 리셋뿐) — 전원 용량·12V 배선 점검 용의. 눌렸을 수 있는
   상태에서 Torque_Limit 을 올리지 말 것(1차 finally 가 350→600 원복해 더 밀었다).
4. **손목캠 상수 재측정** — `y_to_px`·`jaw_offset_z_m`·기준선. 뎁스캠 없이 가능.
5. **정합 재개** — 다음 실물 작업. 죠에 빨간 물체를 물리고(6절 절차) `handeye.py`.
   `POSES` 아래층은 신 floor(-0.078)에 맞춰 -0.05 → **-0.03** 으로 올렸고(여유
   48mm), 프리플라이트가 IK·캘리브 범위·바닥 여유(45mm)를 이동 전에 검사한다.
6. **정합 직후 `floor_from_depth.py` 로 바닥 교차 검증** — 뎁스캠 평면 피팅의
   비접촉 측정. 접촉 탐지(probe_floor)는 이제 예비 수단이다. depth_daemon 에
   `/points` 가 추가돼 서버(데몬) 재시작이 필요하다.

**작업 규칙**: 팔을 움직이는 모든 동작은 **실행 전에 사용자 허락을 받는다.** 토크 ON,
위치 명령, 조그, 캘리브 중 이동 전부 해당한다. 읽기 전용 진단만 예외다. 그리고 자동
이동을 걸어 둔 채 다른 작업으로 넘어가지 않는다 — 이번 사고가 그래서 났다.

---

## 2026-08-20 오후 — 완전 자동 파지 성공, 전원 차단 휴식

위 "다음 단계" 4~6번은 전부 완료됐고, 그 너머까지 갔다.

- **정합 완성**: 이중 솔버(Kabsch + 방위각·책상평면) 교차 대조. 비접촉 바닥
  -0.0734 vs 접촉 -0.078 (4.6mm 일치). Astra SDK 기본 좌우 미러가 정합 발산의
  진범이었다 — astra.py 에서 해제(HANDOFF 위쪽 서술은 미러 이전 기준).
- **완전 자동 파지 성공** (`pick_demo.py lying`): 뎁스캠 재검출 → 교시 오프셋
  보정 → 적응 접근고도 → 저속 하강 → 파지 → 상승. 사용자 확인 "정확하게 파지".
  실패에서 확정된 규약이 코드에 박혀 있다: 그리퍼는 **절대각**(개방 55·폐쇄 1,
  delta 는 이중 개방 사고), 개방 전 **보호해제 goto**(위치 재전송 = STS3215
  과부하 플래그 해제), 파지 후 **압력해제**(목표=현재값, 목표 0 방치 시 수 분 뒤
  Overload 로 열기 거부), 닫힘 ~15초는 `wait_gripper_settle` 폴링.
- **교시 상수** (`servo_gain.json`): `grasp_xy_offset_m=[0.0118,-0.0127]`,
  `wrist_grasp_target_px=[99.3,149.5]` — 사용자가 물체를 죠에 직접 넣어 준
  1회 교시에서 실측. 재정합·카메라 이동 시 재교시.
  **미세 피드백(미반영)**: 하강 중 죠가 물체를 살짝 스침 — "아랫턱이 물체보다
  조금 더 왼쪽에 와도 된다". 다음 실물 세션에서 손목캠으로 방향 확인 후 수 mm.
- **`place_down.py` 신규**: 내려놓기+저고도 휴지(책상+6mm, 토크 유지) — 전원
  차단 대비 자세. 모의 리허설 5사례(`test_place_down.py`) 통과. **실물 미실행**
  — 사용자가 물체를 수동 회수하고 전원을 뽑았다.
- **MuJoCo 미러 신규** (`sim/`): 실팔 /state 10Hz → MJCF qpos, 뎁스캠 물체를
  빨간 원기둥으로 배치. 좌표 적합 RMS 0.00mm (같은 URDF 출신, qpos=URDF q 직결,
  TCP=gripperframe 사이트). 읽기 전용. 사용법은 `sim/README.md`.
- **현 상태**: 전원 차단(사용자, 점심). 팔은 물체 파지 상태에서 전원이 끊겼으니
  재가동 시 자세를 눈으로 확인하고 unfold_safe/토크 순서대로. 서버 프로세스는
  살아 있을 수 있음 — 재가동 전 재시작 권장.
- **다음 후보**: ①빨간 큐브 3cm 출력(`~/Downloads/so101_gcode/cube_only_46m.gcode`)
  후 파지 반복 ②아랫턱 좌측 여유 튜닝 ③파지 판정을 손목캠 기반으로(그리퍼
  각도는 가는 목에서 무효) ④미러 실시간 검증 ⑤**14차 리뷰 이월**: M1(감시가
  ⛔만 보고 '⚠ goto: RxPacketError' 류를 놓침 — 로그 새 줄 전체 검사로),
  M5(도달 판정 gap<1.5° 는 서버 완료 기준 3.0° 보다 엄격해 1.5~3.0° 정착이
  타임아웃 — 서버 '이동 완료' 로그 신호 기반으로). 둘 다 move_and_wait
  재설계라 **실물 검증과 함께** 할 것. 그 외 m: wait_gripper_settle 연속 2회
  안정, 감속 구간 조기판정.

### 2026-08-20 저녁 — 픽앤플레이스 완주 + 온도 레지스터 래치 사건

- **파지 2연속 성공 → 박스 투하까지 완주**: 재가동 후 전자동 파지(그리퍼
  2.9, 손목캠으로 물림 확인 — "물었음/놓친 듯" 블롭 판정은 14mm/임계 15mm
  경계로 또 오판, ③이 여전히 1순위). 사용자가 패널로 투하 지점 교시(pan
  73.5°, 실물 ~13cm 정육면체 박스), 5cm 하강→방출→복귀로 **박스 안 안착**
  확인. 녹화 3종: `~/so101-mobile-manipulation/media/2026-08-20/drop_{wrist,rgb,sim}.mp4`.
- **온도 레지스터 래치 사건**: unfold 중 shoulder_lift 판독이 35→77°C 점프
  후 고정 → 소프트 가드(62°C)가 즉시 토크 컷 → 팔이 접힘 자세로 14° 낙하.
  실체: **오독** (케이스 미지근·수 분간 1°C 불변·부하 최소 자세·전원 리셋
  후 31°C 복귀, 같은 구간 재주행 시 31~33°C). 유발 용의: 전원/통신 순간
  이상 — 눌림 버스 사망 2회와 함께 **12V 어댑터 용량·배선 점검** 근거 추가.
- **과열 대응 등급화** (arm_gui._guard, 사용자 지시 "온도 높다고 토크를
  내려버리면 어떡하나"): 감지 → _do_stop(정지·자세 유지·토크 보존)+경보,
  다음 판독에서 **+2°C 이상 실상승 시에만** 토크 컷. 래치 오독은 정지
  상태로 경보만. 최후 방어선은 펌웨어 과온(65°C). 가짜 버스 4시나리오 통과.
  + _do_connect 포트 재탐색(ACM0↔ACM1 재열거, 실측 4회). **15차 리뷰 대기
  중 — 승인 후 커밋할 것** (실행본에는 이미 반영·가동 중).
- **미러 업그레이드** (커밋 4363514): 롤 표시 오프셋(실물 그리퍼 롤
  반전 조립 — 실물 대조 2회로 -90° 확정, 최초 추정 180°는 오답), 파지 동기화(닫힘<25 → graspframe 부착·방출 시 낙하), 13cm
  투하 박스, --record 무화면 녹화 모드.

### 2026-08-20 밤 — "버스 급사" 진범 확정: 과부하 보호 오발 (커밋 e441726)

급사 4회(눌림 2·이동 중 2)의 원인 규명 완료. **하드웨어 무죄** — 급사 순간
커널 로그에 USB 이벤트가 없고(링크 생존, ipTIME UH505 경유는 사실이나 무관)
어댑터도 12V 5A. 진범은 8/19 소손 대응으로 넣은 **Overload_Torque 60% +
Protection_Time 0.5초**: 중력을 이기는 정상 저속 이동이 그대로 걸려, 보호
진입 서보가 ①토크 20%로 무너지고(주저앉음 = "토크 꺼진 듯") ②에러 패킷
응답으로 일괄 읽기 전체 실패("전 서보 무응답"), 전원 리셋으로만 복구.
데모의 wrist_roll 무반응(가로 파지 롤 회전 중)도 같은 뿌리 — 공장값 완화
후 72° 회전 정상. 온도 쓰레기 판독(77·122)도 보호 상태 패킷 오염 용의.

- PROTECT 완화: Overload 80%·Protection_Time 2초(공장값). **과전류 320·
  과온 65°C 는 유지** — 공장값은 과전류 보호가 0(꺼짐)이라 소손 재발 위험.
- 스톨 대응도 유지형으로: 토크 컷 → 목표=현재 재기록+자세 유지(토크 ON).
- ⚠ 이날 진단 중 규율 위반 사고: 승인 없는 ik 재현 실험이 팔을 책상에
  처박을 뻔함("같은 TCP ≠ 같은 자세"). 팔 이동은 진단 목적이어도 건별
  승인 — [[arm-motion-discipline]] 메모리 참조.

---

## 2026-08-21 — 뎁스캠 팬/틸트 서보 도입, 정합 재시도 5회

카메라를 서보로 겨눌 수 있게 됐다. 그 대가로 **정합이 카메라 각도에 묶였고**,
그 각도를 지키는 규약이 새로 필요해졌다. 정합 자체는 아직 미완(5차 중단).

### 새 하드웨어 — ID 7·8

뎁스캠 팬/틸트 서보 2개를 **팔과 같은 버스**에 물렸다. 공장 기본 ID 가 1·3 이라
`shoulder_pan`·`elbow_flex` 와 충돌해 **넷 다 침묵**했다(같은 ID 둘이 동시에
응답해 패킷이 깨진다). `servo_id.py --from 1 --to 7` 로 옮겼다.

    ID 7  팬(좌우) · ID 8  틸트(위아래)

`servo_id.py` 에 `--from` 을 추가했다 — 여러 서보가 물려 있어도 그 ID 가 유일하면
하나만 재번호한다. `--to` 상한도 6 → 30 으로(주변 서보는 7 이상에 둔다).

### 이 버스에서 배운 함정 넷

**① 위치 제한이 목표를 조용히 클램프한다.** `pan` 의 `Min/Max_Position_Limit` 이
1042~3292 였는데 **실제 가동범위와 무관한 값**이었다(현재 위치가 그 밖에 있었다).
서보는 제한 밖 목표를 거부하지 않고 말없이 잘라내므로, **5° 이동 명령이 69° 회전**이
됐다. 증상만 보면 "명령이 이상하게 먹혔다"로 보이지만 원인은 EEPROM 설정이다.
지금은 0~4095 로 열어 뒀다 — 손으로 실측(`cam_calib.py --record`)한 뒤 다시 잡을 것.

**② 사용 구간이 0/4095 경계를 걸치면 서보로 못 넘어간다.** 작업대 방향이 raw 4080,
현재가 49 였다. 짧은 쪽으로 65 raw(5.7°)면 되는데 그 경로에 경계가 있어, 서보로는
정방향 349° 를 돌아야 했다(케이블이 감긴다). `Homing_Offset` 을 1847 → −152 로 바꿔
판독을 2048 중앙으로 옮겼다 — 경계가 반대편 180° 로 밀린다.

    Present_Position = (엔코더 − Homing_Offset) mod 4096      (실측 확인)
    Homing_Offset 은 부호-크기 11비트 → −2047 ~ 2047 만 유효

**③ 같은 포트를 두 프로세스가 열면 손상된 패킷이 그럴듯한 숫자로 돌아온다.**
패널 서버가 5초마다 자동 재연결을 시도하는 동안 `cam_servo.py` 가 같은 포트를 열어
읽기가 깨졌다. 조용히 틀린 값이 나오므로 예외로도 안 드러난다. 그래서
**카메라 서보를 Worker 가 소유하도록 통합**했고(`arm_gui.CAM`), 별도 도구는
패널 서버가 떠 있으면 **거부**한다(`--force` 로만 우회).

    · bus.motors 에는 등록하지 않는다 — 등록하면 캘리브 로드·정규화·인자 없는
      sync_read·스톨 감시가 전부 카메라까지 건드린다. packet_handler 로 직접 읽고 쓴다.
    · 패널 op: cam_home(기준각 복귀) · cam_move(상대 이동)
    · /state 에 cam{axes, home, at_home} 이 실린다 (2초 주기)

**④ 이동 중에는 "여러 번 읽어 일치 확인"이 원리상 실패한다.** `cam_calib --record` 가
손으로 돌리는 동안 표본을 2.2Hz 까지 놓쳤고, 벌어진 간격이 정상 이동을 경계 넘음으로
오판해 **가동범위 19° 가 379° 로** 나왔다. 기록 중에는 단발로 빠르게 읽고, 경계 판정
임계를 2048 → 3000 으로 올렸다(손으로는 한 표본에 그만큼 못 움직인다).

### 정합은 카메라 각도에 묶인다

`handeye.json` 은 "카메라가 그 자리에 그 자세로" 있다는 전제로 푼 변환이다. 서보로
각도를 바꾸면 무효다. 그래서:

    cam_calib.json  {home: {pan, tilt}}   ← 정합을 푼 기준각
    pick_demo.ensure_cam_home()           ← 파지 시작 시 벗어나 있으면 서버가 되돌린다
                                             (복귀 실패하면 "정합을 믿을 수 없어" 중단)

매번 사람이 맞추는 것은 설계 실패다(2026-08-21 사용자 지시). 카메라가 움직일 수 있는
이상, 시작할 때 스스로 맞춰야 한다.

### 정합 5회 시도 — 매번 다른 원인

| 차수 | 원인 | 결과 |
| --- | --- | --- |
| 1 | 검출 임계를 그늘에 맞춰 완화했더니 **멀리 있는 빨강**(1m)을 큐브로 오인 | RMS 244.8mm |
| 2 | 깊이 필터가 오검출은 막았으나 큐브도 못 봄 | 관측 실패 연속 |
| 3 | 큐브를 죠 끝에 걸쳐 물어 **순회 중 이탈** | 이탈 감지가 중단 |
| 4 | y 음수 지점 7개 관측 실패 | RMS **9.5mm** (게이트 8mm 코앞) |
| 5 | 4차 원인은 **내가 넣은 깊이 필터**였다 | 8/13 관측 성공에서 중단 |

**게이트가 다섯 번 다 잘못된 정합의 저장을 막았다.** 특히 3차의 이탈 감지("로봇
44mm 이동에 카메라 시선 5mm, 2회 연속")가 없었으면 빈 죠로 13지점을 다 돌고 엉터리
변환을 남겼을 것이다.

### 죠에 물린 물체는 책상 위 물체와 다르다

같은 큐브인데 **책상 위에서는 area 1800 으로 잘 잡히고, 죠에 물리면 안 잡힌다.**

    책상 위 큐브   조명 직접 · 가림 없음            → area 1800
    죠 정중앙      죠 몸체가 가림 + 그늘             → 마스크 490px, MORPH_OPEN 5x5 후 0
    죠 끝 걸침     잘 보이지만(area 781) 순회 중 이탈
    죠 옆면 물기   보이면서 유지됨 (area 274~352)   ← 정답

그늘에 든 큐브 실측 `H 163 · S 109 · V 43`, 종전 임계 `H≥166 · S≥140 · V≥55` 는 셋 다
아슬하게 빗나갔다. `H≥158 · S≥120 · V≥45`, `MIN_AREA 80 → 50` 으로 재보정.
**S 를 100 아래로 더 내리면 사람 팔이 큐브보다 큰 덩어리로 잡힌다**(실측: 팔 1489px vs
큐브 523px) — 120/45 가 경계다.

### 깊이가 없는 후보를 버리면 안 된다

멀리 있는 빨강을 거르려고 "면적 최대 하나"에서 "깊이가 작업 범위(0.35~0.85m) 안인
최대 후보"로 바꿨는데, **깊이 유효 화소 5개 미만인 후보까지 버린 것이 4차 실패의
진짜 원인**이었다. 구조광 그림자에 물체가 통째로 들어가면 깊이가 비는데, 그때도
**픽셀 방위각은 유효**해서 정합·파지가 방위각∩평면으로 푼다(원래 설계가 그랬다).

지금은 우선순위가 이렇다: 깊이가 범위 안인 후보 > 깊이가 빈 후보(차선) > 없음.
깊이가 **있는데 범위 밖**인 후보만 버린다.

### 파지 압력 — 완전 해제는 이동 중 이탈을 만든다

파지 후 `goto(현재값)` 으로 압력을 푸는 것은 장시간 과부하 보호를 피하려는 규약인데,
정합 순회처럼 팔이 크게 움직이는 동안에는 유지 토크만으로 부족해 물체가 빠졌다.
순회 중에는 현재값보다 3° 안쪽을 목표로 남겨 **약한 압력**을 건다. 대신 온도를 본다.

### 리치는 상수 상자로 자르지 않는다

`pick_demo` 가 `0.10 ≤ x ≤ 0.28 · |y| ≤ 0.12` 로 작업영역을 검사했는데, IK 격자로
실측하니 실제는 **x 6~25cm, y 는 x 에 따라 최대 ±20cm** 였다. 상수 상자는 잡을 수
있는 자리를 미리 거부했다(검출된 큐브가 x=8.9cm 라 거부됐다). 판정은 IK 가 하고,
못 닿을 때는 **가장 가까운 실제 가능 지점**을 좌표로 안내한다.

### unfold는 계산만 나누고 실행은 한 번에 한다

8° 간격은 접힌 자세에서 죠를 안전하게 띄울 경로를 찾는 계산 단위다. 이 값을 `goto`로
하나씩 보내면 엘보가 걸음마다 멈추며 떨린다. 현재 구현은 계산된 웨이포인트 전체를
`smooth_move`가 15Hz 궤적으로 보간해 한 번에 전송한다. 구간 경계에서는 멈추지 않는다.

    저공 부양 구간             7°/s
    shoulder_lift 한 구간      최대 6°/s
    나머지 작업 자세 구간     13°/s

팬 잠금 중에는 작업 자세의 `shoulder_pan=0°`를 쓰지 않고 잠금 중심을 목표로 삼는다.
`test_unfold_path.py`는 경로 최저 높이, 연속 틱 간 최대 변화, 팬 목표, 숄더 속도를
실물 없이 검증한다.

### 그 밖에 고친 것

    그리퍼 개방   80 → 45     4cm 큐브는 19.6 에서 물린다 — 80 은 죠를 90° 넘게 젖히고
                              여는 데만 20초가 걸렸다
    기본 속도     15% → 50%   위험 구간은 각 스크립트가 그때만 낮춘다
    run_demo.sh   python3 → lerobot 환경 파이썬 (시스템 python3 에 lerobot 이 없어
                  conda 를 활성화하지 않으면 전 단계가 조용히 실패했다)
    미러          큐브 방향(yaw)을 반영 · 손으로 물려준 물체도 인식 · 교시 오프셋 적용
    데이터셋      run_demo 가 에피소드를 자동으로 열고 닫는다 (DEMO_NO_REC=1 로 끔)

### 다음에 할 일

1. **정합 완주** — 큐브를 죠 옆면으로 물리고 `handeye.py`. 5차에서 8/13 관측 성공
   상태였으니 완주하면 게이트를 통과할 가능성이 높다.
2. **큐브 교시** — `teach_cube_offset.py` (아직 한 번도 못 돌렸다)
3. **카메라 가동범위 실측** — `cam_calib.py --record` → `--apply-limits`.
   지금은 제한을 0~4095 로 열어 둔 상태다.
4. 파지 → 대각 큐브 검증 (코드·리허설은 준비됨)
