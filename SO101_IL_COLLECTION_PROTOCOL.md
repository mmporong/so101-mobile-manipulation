# SO-101 빨간 큐브 → 갈색 빈 IL 데이터 수집 절차

- 상태: 수집 프로토콜 확정, 본수집 전 구현 게이트 확인 필요
- 대상: SO-101 리더·팔로워, 손목 카메라, 고정형 RGB 또는 RGB-D 카메라

작업 문장: `Pick the red 4 cm cube and place it inside the brown bin`

## 1. 이 문서의 목적

이 절차는 사람이 SO-101 리더를 조작해 팔로워의 모방학습 데이터를 수집할 때 사용한다. 목표는 보기 좋은 성공 영상 몇 개가 아니라, 같은 초기 조건과 데이터 형식으로 재현 가능한 학습 데이터를 만드는 것이다.

한 에피소드에서 로봇은 닫힌 그리퍼로 시작한다. 안전한 높이에서 그리퍼를 열고, 빨간 큐브를 한 번에 집어 갈색 빈 안에 넣은 다음, 홈 자세로 돌아와 그리퍼를 닫고 끝낸다.

핵심 원칙은 다음 네 가지다.

1. 동작은 부드럽게 이어 간다. 구간을 표시하려고 로봇을 일부러 멈추지 않는다.
2. 파지 방법, 손목 방향, 시작·종료 자세는 모든 에피소드에서 동일하게 유지한다.
3. 변화는 미리 정한 큐브 시작 위치에만 단계적으로 준다.
4. 실패하거나 사람이 개입한 에피소드는 첫 학습 데이터에서 제외한다.

## 2. 먼저 구분할 것: 물리적 멈춤과 논리적 구간

`접근`, `파지`, `운반`, `투입`은 분석을 위한 논리적 구간이다. 구간 경계를 만들기 위해 실제 로봇을 멈출 필요는 없다.

30 FPS에서 1초 동안 멈추면 동일하거나 거의 동일한 행동 프레임 약 30개가 들어간다. 이런 정지가 반복되면 정책도 물체 앞이나 빈 위에서 망설이는 동작을 배울 수 있다. Isaac Lab Mimic 공식 가이드도 긴 시연, 불규칙한 움직임, 시연 중 정지를 대표적인 데이터 생성 실패 요인으로 설명한다.

따라서 다음처럼 구분한다.

| 항목 | 적용 방법 |
| --- | --- |
| 동작 구분 | 녹화 후 이벤트 시점으로 표시 |
| 실제 움직임 | 멈춤 없이 부드럽게 연결 |
| 필요한 안정화 | 그리퍼가 실제로 닫히거나 물체가 떨어지는 물리 시간만 허용 |
| 종료 여유 | 성공 직후 팔을 이탈·복귀시키며 확보 |

## 3. 태스크와 성공 조건

### 3.1 물체

- 집는 물체: 빨간색 40 mm 정육면체
- 놓는 곳: 내부가 빈 갈색 상자 또는 빈
- 빈은 작업 중 밀리거나 회전하지 않도록 고정한다.
- 내부에는 다른 물체를 두지 않는다.
- 큐브와 빈의 실제 크기, 질량, 재질은 본수집 전에 측정해 세션 기록에 남긴다.

갈색 빈의 외형 치수를 추정해서 기록하지 않는다. 다음 값을 자나 캘리퍼스로 직접 잰다.

```text
cube_width_mm
cube_mass_g
bin_outer_width_mm
bin_outer_depth_mm
bin_outer_height_mm
bin_inner_width_mm
bin_inner_depth_mm
bin_inner_height_mm
bin_wall_thickness_mm
```

### 3.2 성공

다음 조건을 모두 만족해야 성공 에피소드다.

- 큐브가 테이블에서 떨어져 파지가 성립했다.
- 큐브가 갈색 빈 내부로 들어갔다.
- 그리퍼가 큐브를 놓았다.
- 큐브가 빈 밖으로 튀어나오지 않았다.
- 로봇이 빈과 물체에서 이탈해 홈 자세로 돌아왔다.
- 홈 자세에서 그리퍼가 닫혔다.
- 전체 과정에 사람의 개입이 없었다.

성공 판정 시점은 두 개로 나눈다.

```text
place_success: 큐브가 빈 내부에 남고 그리퍼가 물체에서 이탈함
episode_complete: 팔이 홈 자세로 돌아와 그리퍼를 닫음
```

마지막 그리퍼 닫기는 투입 성공 조건이 아니라 다음 실행과 같은 초기 상태를 만들기 위한 종료 동작이다.

## 4. 카메라 구성

### 4.1 권장 입력

| 이름 | 위치 | 반드시 보여야 하는 것 |
| --- | --- | --- |
| `images.front` | 작업 공간 앞·위쪽의 고정 카메라 | 홈 자세, 큐브 시작 위치 5개, 빈 전체, 투입 결과 |
| `images.wrist` | 팔로워 손목 | 그리퍼 끝, 큐브, 파지 순간, 빈 테두리 |

- 목표 프레임률: 약 30 FPS
- 최소 해상도: 640×480
- 카메라 위치, 초점, 노출과 조명을 한 수집 세션에서 바꾸지 않는다.
- 리더 팔, 사람의 손과 몸이 영상에 들어오지 않게 한다.
- 사람이 카메라 영상만 보고도 작업 성공 여부를 판단할 수 있어야 한다.

### 4.2 Depth 처리

첫 ACT 또는 BC 기준 모델에는 RGB와 관절 상태만 사용한다. Depth가 있다면 원본을 보존하되 첫 모델의 필수 입력으로 만들지 않는다.

현재 `ds_record.py`의 `observation.images.depth`는 이름과 달리 RGB-D 카메라의 컬러 JPEG를 기록한다. 이 데이터를 실제 깊이값이라고 설명하면 안 된다. 실제 깊이를 정책 입력이나 분석에 사용하려면 raw depth, 단위, invalid pixel 처리, RGB-depth 정렬 여부를 별도 피처와 manifest로 기록해야 한다.

## 5. 수집 전 구현 게이트

현재 `teleop_record.py`를 그대로 본수집에 사용하기 전에 다음 차이를 해결해야 한다.

| 현재 구현 | 이번 태스크에 필요한 상태 |
| --- | --- |
| task가 `pick the red cube and place it on the table`로 고정 | `Pick the red 4 cm cube and place it inside the brown bin` 사용 |
| 데이터셋 기록률이 `fps=10`으로 고정 | 목표 FPS를 인자로 받고 실효 기록률을 검증 |
| `--hz`는 리더 텔레옵 주기이며 데이터셋 FPS와 별개 | 두 값을 보고서에서 구분 |
| 에피소드 종료 입력은 저장 동작 | 실패한 현재 에피소드를 버리고 다시 찍는 명확한 입력 필요 |
| 손목 영상과 `depth` 컬러 영상 중심 | 카메라 의미와 실제 피처 이름을 manifest에 명시 |

이 표는 본수집을 완료했다는 뜻이 아니다. 코드와 실행 로그로 각 항목을 확인한 뒤 10회 파일럿을 시작한다.

## 6. 작업 공간 준비

### 6.1 위치 표시

테이블에 제거 가능한 테이프로 다음 위치를 표시한다.

```text
              D: 뒤

    B: 왼쪽   A: 중앙   C: 오른쪽

              E: 앞
```

- A는 로봇이 가장 안정적으로 집을 수 있는 기준 위치다.
- B~E는 A에서 약 20 mm 떨어진 위치로 시작한다.
- 20 mm 변화가 기존 안전 작업 범위를 벗어나면 간격을 줄인다.
- 빈은 한 위치에 고정한다.
- 첫 50회 동안 카메라와 빈 위치는 바꾸지 않는다.

### 6.2 로봇 초기 상태

- 팔로워: 충돌 없이 검증된 홈 자세
- 리더: 팔로워와 비슷한 자세에서 연결
- 그리퍼: 캘리브레이션된 닫힘 위치
- 큐브: 해당 에피소드의 지정 시작점
- 빈: 비어 있고 고정된 상태

그리퍼를 기계적 끝까지 힘으로 밀지 않는다. `열림`과 `닫힘`은 캘리브레이션된 동일한 명령값을 모든 에피소드에서 사용한다.

## 7. 에피소드 표준 동작

### 7.1 리셋: 녹화 밖에서 수행

1. 로봇 동작이 멈춘 상태에서 큐브를 다음 시작 위치에 놓는다.
2. 갈색 빈 내부를 비운다.
3. 카메라 화면에서 큐브, 그리퍼와 빈이 보이는지 확인한다.
4. 팔로워와 리더를 홈 자세에 맞춘다.
5. 그리퍼가 닫혀 있는지 확인한다.
6. 사람 손이 작업 공간과 영상에서 빠진 뒤 녹화를 시작한다.

리셋 동작과 사람의 개입은 학습 데이터에 기록하지 않는다.

### 7.2 녹화 시작과 그리퍼 열기

1. 닫힌 홈 자세에서 녹화를 시작한다.
2. 0.5초 안에 자연스럽게 움직이기 시작한다.
3. 그리퍼가 큐브에서 충분히 떨어진 안전 높이에 있을 때 연다.
4. 큐브 위로 하강하기 전에는 표준 열림 위치에 도달해야 한다.

그리퍼를 열면서 큐브 방향으로 이동할 수는 있다. 다만 손가락이 큐브나 테이블에 닿을 수 있는 영역으로 들어가기 전에 개방을 끝낸다.

### 7.3 접근

1. 열린 그리퍼로 큐브 위쪽에 접근한다.
2. 이동 중에 손목 방향을 파지 방향으로 맞춘다.
3. 큐브 중심 위에서 속도를 줄이고 수직으로 하강한다.
4. 정렬을 확인하려고 오래 멈추지 않는다.

좋은 접근은 `이동 → 정지 → 방향 수정 → 정지 → 하강`이 아니라 `이동하면서 정렬 → 감속 → 수직 하강`이다.

### 7.4 파지와 리프트

1. 파지 높이에 도달하면 팔의 큰 움직임을 줄인다.
2. 그리퍼를 한 번에 세게 닫지 말고 연속적으로 닫는다.
3. 큐브가 물리면 바로 수직으로 들어 올린다.
4. 테이블과 충분한 간격이 생기기 전에는 옆으로 움직이지 않는다.
5. 안전 높이에 도달한 뒤 빈 방향으로 전환한다.

그리퍼가 실제로 닫히는 동안의 짧은 시간은 정상 동작이다. 파지 후 별도로 1~2초 대기하는 동작은 넣지 않는다.

첫 논리적 구간의 종료는 그리퍼가 닫힌 순간이 아니라 큐브를 파지해 안전 높이까지 올린 뒤다.

```text
event: grasp_and_lift_complete
```

### 7.5 운반

1. 큐브를 빈 테두리보다 높은 안전 높이로 유지한다.
2. 빈 입구 중앙으로 한 번에 이동한다.
3. 이동하면서 큐브 중심과 빈 중심을 맞춘다.
4. 빈 위에서 별도의 정지 구간을 만들지 않는다.

손목 방향과 이동 경로는 모든 성공 시연에서 가능한 한 동일하게 유지한다. 서로 다른 파지 전략은 첫 기준 데이터셋이 성공한 뒤 별도 실험으로 추가한다.

### 7.6 투입

1. 큐브 중심을 빈 입구 중앙에 맞춘다.
2. 그리퍼와 손목은 가능한 한 빈 테두리 위에 둔다.
3. 큐브가 빈 안으로 들어갈 수 있는 높이에서 그리퍼를 부드럽게 연다.
4. 큐브가 손가락에서 빠지는 동안 옆으로 움직이지 않는다.
5. 큐브가 떨어지면 열린 상태로 수직 이탈한다.

첫 기준 태스크는 빈 바닥에 정밀하게 내려놓는 삽입 작업이 아니다. 그리퍼를 벽 안쪽 깊이 넣지 않고 중앙 위에서 안전하게 놓아 떨어뜨리는 `pick-and-drop into bin`으로 정의한다.

```text
event: place_complete
```

### 7.7 이탈과 종료

1. 열린 그리퍼로 빈에서 수직 50 mm 이상 이탈한다.
2. 빈과 큐브를 건드리지 않는 경로로 홈 자세에 복귀한다.
3. 홈 자세에서 그리퍼를 캘리브레이션된 닫힘 위치로 닫는다.
4. 닫힘이 확인된 뒤 약 0.5초의 종료 여유를 둔다.
5. 에피소드를 저장하고 녹화를 끝낸다.

종료 여유는 성공 직후 데이터를 잘라내지 않기 위한 것이다. 빈 위에서 오래 정지하는 방식으로 만들지 않는다.

## 8. 한 줄 운용 리듬

운영자는 다음 순서를 동일하게 반복한다.

```text
닫힌 홈에서 시작
→ 안전 높이에서 열기
→ 부드럽게 접근·하강
→ 한 번에 파지
→ 바로 수직 상승
→ 빈 중앙으로 운반
→ 열어서 투입
→ 열린 상태로 수직 이탈
→ 홈 복귀
→ 닫기
→ 종료
```

## 9. 저장하지 않고 다시 찍는 조건

다음 중 하나라도 발생하면 첫 성공 데이터셋에는 저장하지 않는다.

- 파지 전에 1초 이상 망설였다.
- 큐브를 밀거나 쓰러뜨린 뒤 다시 잡았다.
- 두 번 이상 재파지했다.
- 갑작스럽고 큰 관절 움직임이 있었다.
- 큐브를 충분히 들기 전에 옆으로 끌었다.
- 그리퍼나 큐브가 빈 벽과 크게 충돌했다.
- 큐브가 빈 밖으로 떨어지거나 튀어나왔다.
- 사람이 동작 중 개입했다.
- 리더 팔이나 사람 몸이 카메라에 들어왔다.
- 카메라가 흔들리거나 큐브가 화면에서 사라졌다.
- 기록 중 카메라나 관절 데이터가 끊겼다.
- 실제 수행 태스크와 `task` 문자열이 다르다.

실패 사례가 연구에 필요하면 성공 학습 데이터와 다른 dataset ID에 보존한다. 성공과 실패를 같은 기준 데이터셋에 섞지 않는다.

## 10. 수집 수량

### 10.1 파일럿 10회

10회는 학습 성능을 주장하기 위한 수량이 아니라 수집 파이프라인 검증용이다.

| 시작 위치 | 횟수 |
| --- | ---: |
| A: 중앙 | 10 |

10회가 모두 저장되면 곧바로 학습하지 않고 먼저 11개 품질 항목을 점검한다.

### 10.2 기준 데이터 50회

파일럿이 통과하면 카메라와 빈은 그대로 두고 큐브 위치만 바꾼다.

| 시작 위치 | 횟수 |
| --- | ---: |
| A: 중앙 | 10 |
| B: 왼쪽 | 10 |
| C: 오른쪽 | 10 |
| D: 뒤 | 10 |
| E: 앞 | 10 |
| 합계 | 50 |

LeRobot 공식 가이드는 물체를 여러 위치에서 집어 빈에 넣는 시작 과제에 최소 50회, 위치당 10회를 권장한다. 첫 정책이 안정적으로 파지한 뒤에만 파지 방향, 조명과 카메라 위치 같은 변화를 추가한다.

ROBOTIS `cyclo_lab`의 10개 시연은 최종 학습 데이터 10개를 뜻하지 않는다. 공개 예제는 10개를 annotation한 뒤 Isaac Lab Mimic으로 300회 또는 1,000회 생성 시도를 하고, 생성된 데이터로 BC를 학습한다.

## 11. 파일럿 10회 품질 검사

각 에피소드를 재생하면서 다음을 확인한다.

```text
[ ] task 문자열이 실제 작업과 같다.
[ ] 6개 관절 state와 action이 모든 프레임에 있다.
[ ] 시작 자세가 닫힌 홈 자세다.
[ ] 그리퍼가 큐브 접근 전에 열렸다.
[ ] 큐브가 한 번에 파지됐다.
[ ] 리프트 후에 수평 이동했다.
[ ] 큐브가 빈 중앙으로 들어갔다.
[ ] 팔이 이탈하고 홈으로 복귀했다.
[ ] 종료 시 그리퍼가 닫혔다.
[ ] 카메라에서 핵심 동작이 가려지지 않았다.
[ ] 기록률, 프레임 수와 `dup_pct`가 함께 저장됐다.
```

`ds_record.py`의 `dup_pct`는 같은 관절 상태가 반복 기록된 비율이다. 값 자체만 보고 합격선을 임의로 정하지 말고, 텔레옵 주기·기록 FPS·카메라 지연과 함께 본다. 목표 FPS보다 실효 기록률이 크게 낮거나 특정 구간의 프레임이 반복되면 본수집을 중단하고 원인을 고친다.

## 12. 세션 기록

수집 세션마다 다음 정보를 별도 manifest에 남긴다.

```text
session_id
date_time
operator
git_commit
working_tree_dirty
lerobot_version
robot_id
leader_port
follower_port
calibration_file_sha256
dataset_repo_id
task
teleop_hz
dataset_target_fps
dataset_effective_fps
camera_features
camera_resolution
cube_dimensions_mm
cube_mass_g
bin_outer_dimensions_mm
bin_inner_dimensions_mm
lighting_condition
episode_count_attempted
episode_count_saved
episode_count_rejected
rejection_reasons
```

대용량 MP4, Parquet, Depth와 checkpoint는 Git에 넣지 않는다. 데이터셋 URI, commit 또는 revision, 파일 해시와 작은 요약만 저장소에 남긴다.

## 13. 안전과 중단 조건

- 로봇이 움직이는 동안 손을 작업 공간에 넣지 않는다.
- 큐브와 빈의 재배치는 팔이 정지한 리셋 구간에서만 한다.
- 빈은 미끄러지지 않게 고정한다.
- 텔레옵 시작 전에 팔로워와 리더 자세를 맞추고 시작 램프를 끝까지 확인한다.
- 바닥 가드, 관절 범위 또는 패널 안전 게이트가 거부하면 우회하지 않는다.
- 통신 실패, 비정상 소음, 지속 눌림이나 예상하지 못한 움직임이 보이면 즉시 중단한다.
- 현재 `teleop_record.py`에서 `Ctrl-C`는 현재 기록을 폐기하고 정지 명령을 보내지만 토크는 유지한다. 임의 자세에서 전원을 끄지 말고 검증된 종료·파킹 절차를 사용한다.

이 문서는 실제 이동 명령을 자동 실행하지 않는다. 실물 운용은 사용자가 현장에서 작업 공간을 확인하고 텔레옵을 시작한 경우에만 수행한다.

## 14. 학습 시작 게이트

다음 조건을 모두 만족한 뒤 ACT 또는 BC 학습을 시작한다.

- 파일럿 10회가 품질 검사 통과
- 본수집 50개 성공 에피소드 확보
- task 문자열과 로봇 ID가 전 에피소드에서 동일
- 카메라 피처와 해상도가 동일
- state/action 차원과 관절 순서가 동일
- 실패 에피소드가 성공 기준 데이터셋에서 제외됨
- 학습용 위치와 평가용 위치가 기록으로 구분됨

10개만으로 학습을 돌리는 경우 결과는 `pipeline smoke test`로 표시한다. 최종 성능이나 10-shot 성공으로 표현하지 않는다.

## 15. 포트폴리오에 남길 증거

- 작업 공간 전체 사진과 카메라 배치도
- 큐브와 빈 실측 치수
- 5개 큐브 시작 위치 도면
- 파일럿 10회 품질 검사 결과
- 50회 수집 성공·거부 현황
- 실제 FPS와 중복 프레임 통계
- 데이터셋 revision과 SHA-256
- 성공 및 실패 예시 영상
- ACT 또는 BC 학습 설정과 checkpoint
- 학습에 쓰지 않은 위치에서의 실물 평가 결과

포트폴리오에서는 `수집함`, `학습함`, `실물 평가함`을 분리해서 기록한다. 성공 영상 하나로 정책 성공률을 주장하지 않는다.

## 16. 근거 자료

- LeRobot, Imitation Learning on Real-World Robots: https://huggingface.co/docs/lerobot/main/il_robots
- LeRobot, What Makes a Good Dataset: https://huggingface.co/blog/lerobot-datasets
- LeRobot SO-101 recording implementation: https://github.com/huggingface/lerobot/blob/main/examples/isaac_teleop_to_so101/record.py
- Isaac Lab Mimic teleoperation and data generation: https://isaac-sim.github.io/IsaacLab/v2.2.1/source/overview/imitation-learning/teleop_imitation.html
- ROBOTIS cyclo_lab: https://github.com/ROBOTIS-GIT/cyclo_lab
- ACT paper: https://arxiv.org/abs/2304.13705

## 17. 현장용 짧은 체크리스트

```text
수집 전
[ ] 큐브 위치 확인
[ ] 빈 고정·내부 비움
[ ] 카메라 두 시점 확인
[ ] 리더·팔로워 정렬
[ ] 닫힌 홈 자세 확인
[ ] task와 dataset ID 확인

녹화
[ ] 안전 높이에서 열기
[ ] 멈추지 않고 접근
[ ] 한 번에 파지
[ ] 수직으로 먼저 들기
[ ] 빈 중앙으로 운반
[ ] 열어서 투입
[ ] 열린 상태로 수직 이탈
[ ] 홈 복귀 후 닫기
[ ] 종료 여유 후 저장

거부
[ ] 재파지·충돌·망설임 없음
[ ] 사람 개입 없음
[ ] 카메라·관절 누락 없음
[ ] 큐브가 빈 내부에 남음
```
